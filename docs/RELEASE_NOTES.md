# HxSync 0.1.19（开发预览）

**用途**：作者自用与协作者试用预览包。本版存档「④ 真 Agent loop」主线，便于后续开「三关」中间态等体验迭代。

## 本版重点

- **④ Agent loop**：端侧网关 API 路径多步续跑——工具确认后把结果回灌模型继续（最多 8 步），不再只是单轮 chat
- **路由**：优先云端大脑；本地仅承接极短寒暄
- **本机工具扩展**：`url.open`、`web.search`、`share.text`（仍须确认）
- **忙态 UI**：思考中用 spinner，干活中用键盘图标（替代流式 `|` 光标）
- **Prompt**：强化 Agent 人格与多步工具说明

延续 0.1.18：Edge 小艺朗读、历史按 Agent、错误中文化等。

## 安装

```bash
./gradlew :app:assembleRelease
```

同 release 签名可直接覆盖；debug → release 须先卸载。

---

## 历史版本

### 0.1.18

- Edge 小艺朗读；朗读引擎可选；云端 404 不再粘顶栏；自动模式静默回退

### 0.1.17

- 历史按 Agent；错误中文化；朗读修复；深色夜间；历史删除；HF 401 提示；系统返回

### 0.1.16

- 聊天主题驱动背景与壁纸相册/搜索；朗读修复；历史删除；深色夜间；HF 401 提示

### 0.1.15

- 本地多会话历史；配置助手裸 Key；本地模型暂停与断点续传

### 0.1.14

- ④ 端侧网关自动 / 优先本地 / 优先云端
