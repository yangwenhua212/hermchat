# HxSync 愿景与架构边界

工程师备忘：定调「做什么 / 不做什么」。产品细则见 [docs/PRODUCT.md](docs/PRODUCT.md)，分步见 [docs/ROADMAP.md](docs/ROADMAP.md)。

---

## 一句话

**HxSync 首先是口袋里的 Agent 客户端（四档调度 + 本机手脚 + 人在回路）。**  
具身机器人、IoT 等是**并行扩展的执行器类型**，挂在同一套编排与确认模型上——**不替代、不架空** ①②③④ 主线。

---

## 架构定位（基石不变）

四档永远是产品基因：

| 档 | 角色 |
|----|------|
| ① 本地 | 弱网/隐私向的本机推理与反射式能力 |
| ② API | 云端规划保底 |
| ③ 远端 Agent | PC/边缘完整大脑（WS / Hermes 等） |
| ④ 端侧网关 | 本地决策编排 + 本机（及将来外设）工具 |

所有扩展遵循开闭原则：

- 对现有聊天 / Agent / 本机工具主线：**关闭无谓修改**（不为追热点重写基本盘）。
- 对新增执行器类型（机器人、IoT 等）：**开放扩展**（新包、新工具名、新协议薄层）。

---

## 两翼并进

```
                    ┌─────────────────────────┐
                    │   HxSync 编排中枢         │
                    │  四档 · Loop · 确认卡 · UI │
                    └───────────┬─────────────┘
                                │
              ┌─────────────────┴─────────────────┐
              ▼                                   ▼
    ┌──────────────────┐              ┌──────────────────┐
    │ 主线翼（基本盘）   │              │ 扩展翼（并行探索） │
    │ 聊天 / 本机工具   │              │ robot.* / 将来 IoT │
    │ ①②③④ 日常交付    │              │ 竖切真机再加厚     │
    └──────────────────┘              └──────────────────┘
```

| | 主线翼 | 扩展翼（具身等） |
|--|--------|------------------|
| 价值 | 给人办事的 Agent 客户端 | 给实体机器人当「端侧编排大脑 + 安全闸门」 |
| 迭代 | 继续打磨连接、Loop、真机矩阵、PITFALLS | 另增协议与工具；复用 Loop / 确认 / 多档 |
| 发版 | 主节奏 | 不抢主线；未交付不写进「已具备」 |

「并行探索」比「远期替代路线」更准：主线形状不被机器人故事改写；机器人是客户端扩展出的一类**硬件外设执行器**。

---

## 具身线（执行器扩展）

机器人是 ④ 调度层（及同一套工具确认环）下的**新执行器**，继承：

- **Loop**：分析 → 确认 → 执行 → 观察（复用）
- **安全模型**：高危动作走确认卡（复用）
- **多档弹性**：① 本地 / ② 云 / ③ 远端仍可接管规划；手机主持闭环，不是单纯摇杆

### 竖切优先（探索验收）

一台 ROS 2 小车 + 少量 `robot.*`（如前进/停止）+ 确认后执行 + 屏上可见阶段。  
协议优先薄 Bridge，忌一上来全量重写。

### 代码边界（演进）

- 新增能力放独立包/命名空间（如 `executor/robot` 或 `tools` 下 `robot.*`），**不污染**现有手机工具实现去「改成万能 Action 大爆炸」。
- 统一「执行器」抽象是**演进目标**：Loop / 确认卡 / 调度先复用；真机竖切跑通后再抽接口。禁止为愿景空转大重构。
- 运动反射在车端实时层；手机不做毫秒级硬实时神话。

细则与「不做什么」：[docs/ROADMAP.md](docs/ROADMAP.md#并行探索hxsync-for-robotics具身--另开一条能力线)。

---

## 与主线的边界（硬规则）

1. 主线（聊天 / 本机工具 / ③④）照常迭代，**不因机器人线重构**。  
2. 机器人线只**新增**协议与工具，默认不改四档产品语义。  
3. 用户可见「已具备」表与 Release 说明：未竖切交付前，不把机器人写成现成功能。  
4. 主线带宽优先：真机矩阵与稳定性 > 具身探索。

---

## 认知一句话

不是「碰巧带了机器人接口的聊天 App」，也不是「放弃聊天去做机器人 OS」。  
而是：**从一开始就按「可扩展执行器的 Agent 客户端」长**——今天执行器是闹钟与开应用，明天可以是底盘与夹爪；发动机（四档 + Loop + 确认）尽量不动。
